import { ComingSoon } from "@/components/admin/coming-soon";

export default function TablesAdminPage() {
  return <ComingSoon title="Tables" description="Replace the 4 seeded CSV lookup tables. UI not yet built — the API endpoints at /api/admin/tables are functional." />;
}
