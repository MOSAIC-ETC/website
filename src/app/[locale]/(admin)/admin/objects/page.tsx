import { ComingSoon } from "@/components/admin/coming-soon";

export default function ObjectsAdminPage() {
  return <ComingSoon title="Objects" description="Manage paired preview + cube uploads. UI not yet built — the API endpoints at /api/admin/objects are functional." />;
}
