import { ComingSoon } from "@/components/admin/coming-soon";

export default function AuditAdminPage() {
  return <ComingSoon title="Audit log" description="Browse all admin actions. UI not yet built — the API endpoint at /api/admin/audit is functional." />;
}
