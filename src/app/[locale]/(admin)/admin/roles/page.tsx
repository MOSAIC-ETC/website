import { ComingSoon } from "@/components/admin/coming-soon";

export default function RolesAdminPage() {
  return <ComingSoon title="Roles" description="Create roles and tune their permission set. UI not yet built — the API endpoints at /api/admin/roles are functional." />;
}
